# Changelog

## v0.1.2

- **团队模式** — 多模型接力协作，起草 → 修订 → 润色 → 审查，产出一份经过多重打磨的文档
- **合并命令** — 输入 `/merge`，将各模型已有的回答合并为一份综合文档，标注共识和分歧
- **Shift+Tab 切换** — 广播模式与团队模式一键切换（也可用 `/team` 命令）
- **项目重新定位** — 从编程助手转向通用内容生成平台，覆盖文档写作、分析报告、方案设计等场景
- **README 全面重写** — 面向客户，清晰说明两种模式及各自用法

## v0.1.1

- 终端 UI 全面打磨：快捷键提示、错误着色、token 用量实时显示
- 完善测试体系：24 个测试文件、164 个测试用例全数通过
- MiniMax、DeepSeek Provider 支持
- 上下文水位限制可配置，Provider 请求超时与重试
- 启动时自动清理孤儿 worktree
- 滚动偏移、历史输入导航（↑↓）、配置校验警告
- `q` 退出自动保存会话，`--resume` 恢复历史会话
- `--help` / `--version` CLI 参数

## v0.1.0

- 首个可用版本
- **输入栏** — 支持输入指令，一键发送到所有接入模型
- **广播模式** — 多模型同时响应，回答分区显示
- **定向模式** — 选择任一可用模型，仅向该模型发送指令
- **6 家 Provider** — Anthropic、OpenAI、Google、DeepSeek、MiniMax、Ollama
- **git worktree 隔离** — 每个模型运行在独立的 `git worktree` 中，文件系统无冲突
- **内置工具** — bash、read、write、edit、glob、grep
- **权限管理** — 回话内记录已授权项目，无需重复授权
- **Tab 驱动交互** — 一键切换对话目标
- **TOML 配置** — `.multiarenarc` 配置文件，支持 `${ENV}` 环境变量，支持全局 + 项目级覆盖
- **对话历史** — 每个模型独立维护完整上下文
